# 本番検証ループ 実行計画(タスク: 実利用シナリオで使い倒す)

方針(ユーザー指示 2026-07-17): 「修正ループは軽めに書いてあるけど、そこは本番です。
機能面やUI、本当に使ってもらえるかどうかまで、徹底的に検証してください。
必要なら操作もしてください」

## 体制

- **Fable = 人間役+ディレクター役の両方を実演**: ブラウザペインで UI を実操作し、
  CLI・スキル手順(SKILL.md の想定フロー)どおりに最初から最後まで通す
- **Codex = 並行レビュー**(機能の抜け+ライブ UI のコピー/情報設計。computer use 可)
- **修正の采配**: 判断が要る修正=Fable が原因確定し指示、実装=Sonnet。
  修正後は**同シナリオを頭から再走**

## 摩擦ログ形式

`docs/verification-log.md` に追記:
`{シナリオ#, ステップ, 事象(クリック数/迷い/行き止まり/壊れた状態/遅延/コピー不明瞭), 重大度, 根拠(スクショ/コマンド), 修正先}`

- P0: 進行不能・データ破壊 / P1: 大きな摩擦・誤解を生む / P2: 改善余地 / P3: 好み

## シナリオ(すべて実素材・実キット)

1. **週次 vlog ラフカット** — /Users/ht/Movies/Claude_Vlog/projects/2026-07-10/ の
   素材3本を D&D で取り込み → scenes → detect → 候補承認(UI) → 字幕+手直し →
   BGM+ダッキング → QC → render。「いい感じのところを切って」で方針宣言→カットまで
2. **売れそうなところだけショート** — シナリオ1 のプロジェクトから
   「売れそうなところだけ選んでショートにして!」→ 分析→方針宣言→候補提示(show)→
   承認 → 9:16 リフレーム → --preset shorts で書き出し
3. **字幕の手直し** — UI だけで誤字修正・スタイル/フォント/位置変更 →
   web 表示と render 焼き込みの一致を目視比較
4. **週またぎ再開** — デーモン停止→新セッション想定で `vedit resume`(+notes)だけを
   頼りに文脈復元 → 続きの編集が迷いなく始められるか
5. **Resolve 引き渡し** — export otio+srt → smoke:export 検証 + export-guide の
   手順どおりに構造検査(Resolve 実機があれば computer use で実 import)
6. **ぽんしゃすアニメ 45秒** — ~/Documents/vedit-kits/ponshasu で compose:
   3シーン(背景切替)・キャラ2体・セリフ8・SE・BGM → プレビュー確認 → shorts 書き出し

## 合格基準(全シナリオ)

- P0/P1 摩擦ゼロで頭から通る
- 会話+UI だけで公開品質まで到達できる
- 「何をすればいいか分からない」瞬間がない(次の一手が常に画面か応答に見えている)
- ユーザー可視の場所に内部 ID・機械語が出ない

## 前提(着地待ち)

UI 再設計 / 編集メモ / W7 焼き込み / レジストリ剪定 / アニメ棚卸し反映の
フィックス波がマージされてから開始。ロードマップ v3(棚卸し3本の統合)で
追加された P1 修正も先に消化する。
