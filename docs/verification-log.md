# 本番検証ログ

形式: {シナリオ, ステップ, 事象, 重大度, 根拠, 修正先}。docs/verification-plan.md 準拠。

## シナリオ1: 週次vlogラフカット(2026-07-17 実施)

環境: 実素材3本(4K HEVC 25.7s/40.0s/16.1s、リンク取り込み)、scratchpad/verify-s1。

**通ったもの(摩擦ゼロ):** 取り込み3本(プロキシ・波形・シーンサムネ・自動配置、
rev3)→ UI 初期状態(空状態文言・Claude待機中・16:9追従・実サムネカード)→
detect 21件 → 会話バブル+提案カード(日本語ラベル・内訳・−33.6秒)→
個別「カットする」(rev4、尺·ピル·バブル即同期)→ 一括「無音候補20件を
カット」(rev5、1:21.1→0:48.3、confirm 通過、待機中へ復帰)。

| # | ステップ | 事象 | 重大度 | 根拠 | 修正先 |
|---|---|---|---|---|---|
| F-s1-1 | 個別カット承認 | 候補がクリップ冒頭0.1sから始まる場合、先頭に0.1秒の断片クリップが残る(素材1 · 0:00.1)。以後の一括でも 0.1〜0.4s 断片が多数発生 | P2 | rev4 後のタイムライン(クリップ一覧に 0:00.1/0:00.3 等) | ops: removeSourceRange で minClip 閾値未満の断片を隣接カットに吸収 |
| F-s1-2 | 文字起こしタブ | per-source 転写ボタンは存在するが行テキストにしか見えない(「未実行」の行=ボタンだと気づけない。説明文は「上のボタンで」と言う) | P2 | スクショ(文字起こしタブ) | web: 行内に明示的なボタン様式(枠+ラベル「文字起こしする」) |
| F-s1-3 | detect の適用判断 | 発話のない街歩き素材に無音検出を使うと断片化したカット列になる(編集判断としては scenes/culling が適切)。システムは何も警告しない | P2(知見) | rev5 のクリップ断片化 | プレイブック強化+将来: 発話密度が低い素材で detect 実行時に「シーン選別の方が向いています」ヒント |
| **F-s1-4** | 最終レンダー | **whisper の幻覚タグ「[MÚSICA DE FUNDO]」がそのまま字幕として焼き込まれた**(非発話音声の転写でブラケット系タグが cue 化)。低信頼フラグは上がっていたが書き出しは素通し | **P1** | verify-s1-final.mp4 の 0:42〜47 フレーム | core: 非発話タグ([…]/(…)/♪ 等の多言語パターン)を cue 生成から除外+除外件数を render warnings に載せて結果カードへ |
| F-s1-5 | 文字起こしタブ | 転写ボタンが初回表示時に見えない(転写完了後の再描画では明示ボタンが出た)。初回描画経路の様式不一致の疑い | P3(要再現) | スクショ2枚の差 | web: 未実行行のボタン様式を常時明示 |

**シナリオ1 判定: 合格** — 取り込み→検出→承認→転写→BGM→QC→4K書き出し→
結果カードまで会話+UIで完走、行き止まりゼロ、P0ゼロ。F-s1-4(幻覚字幕)は
修正(2eaa2a1)後の再レンダーで焼き込み消滅+警告表出を確認しクローズ。

## シナリオ2: 売れそうなところだけショートに(2026-07-17 実施)

方針宣言(発話なし素材のため視覚基準): 交差点の引き→人混みとスクリーン→
ネオン街歩き の3カット約21秒。派生プロジェクト作成→clip-add×3→
reframe 9:16(center)→ shorts プリセット書き出し。

結果: **合格** — 1080×1920・21.1s・3カットとも中央フレーミング正常。

| # | ステップ | 事象 | 重大度 | 修正先 |
|---|---|---|---|---|
| F-s2-1 | 派生作成 | W6(variant fork)未実装のため再取り込み+再転写が必要(リンクなので容量影響なし、時間コストのみ) | P2(既知) | ロードマップ W6 |
| F-s2-2 | CLI 操作 | clip-add/reframe の構文ミスに明確な usage が返り自己回復可能だった(良い挙動の記録) | — | — |
| F-s2-3 | 選定品質 | カット1と3の絵柄が近い(同ロケの引き絵)。scene index の類似度情報があれば提案時に回避可能 | P3(知見) | 将来: シーン類似度で多様性チェック |

## シナリオ3: 字幕の手直し(2026-07-18 実施、shibuya-final)

**合格分:** cue クリック→字幕スタイルビュー(クリックした cue の実テキストで
ライブプレビュー)→プリセット変更(クリーン→ボールド)→適用が版23として
永続化。フォントは OS フォント一覧から選択可能。

| # | ステップ | 事象 | 重大度 | 修正先 |
|---|---|---|---|---|
| **F-s3-2** | 発話箇所へ移動 | **単語クリックがシークしない**(選択のみ)。cue クリックはスタイルを開く。発話の瞬間へ飛ぶ手段が UI に無い(クリップクリックの粗いシークのみ) | **P1** | web: 単語クリック=シーク+選択(または修飾キー分離)を復元 |
| **F-s3-3** | クリップクリック後 | video.currentTime は動く(8.16s)のに headerTc / tcNow が 0:00.0 のまま=表示状態の不整合(字幕スタイルビュー開閉後に発生。要再現調査) | **P1疑い** | web: シーク経路の tl 状態更新を追う |
| F-s3-4 | 一時停止中の cue 表示 | tl7.5 で cue が stage に出ない(cue 窓外の可能性もあり F-s3-3 と絡んで未切り分け) | P2(要調査) | 同上とセットで |
| F-s3-5 | フォント一覧 | 先頭に `.Al Bayan PUA` 等の隠しシステムフォントが混入 | P3 | web: ドット始まりを除外 |
| F-s3-1 | a11y | B-roll/モーションのドラッグ用 title に生ファイル名+内部ID残留 | P3 | web: sourceLabel 適用 |

per-cue テキスト編集(ステージ cue ダブルクリック)は F-s3-2/3 に阻まれ未検証
→ 修正後に再走。

## シナリオ4: 週またぎ再開(2026-07-18 実施、verify-s1)

**合格** — note(policy/decision/todo)を書き→`vedit resume` 一発で
プロジェクト状態・版履歴(行為者つき)・方針/未完了TODO/直近判断が返る。
新セッションが迷いなく再開できる情報密度。摩擦なし。

## シナリオ5: Resolve 引き渡し(2026-07-18 実施、shibuya-final)

**合格** — export otio+srt → 実 opentimelineio でパース成功
(V1×4 / A1×4 / V2 B-roll+Gap、439f@29.97=14.6s 一致、SRT 19行)。
CLI 出力に Resolve への取り込み手順が明記される。実機 Resolve import は
環境なしのため構造検査で代替(export-guide の手順どおり)。
