# 本番検証ログ

形式: {シナリオ, ステップ, 事象, 重大度, 根拠, 修正先}。docs/verification-plan.md 準拠。

## シナリオ1: 週次vlogラフカット(2026-07-17 実施)

環境: 実素材3本(4K HEVC 25.7s/40.0s/16.1s、リンク取り込み)、scratchpad/verify-s1。

**通ったもの(摩擦ゼロ):** 取り込み3本(プロキシ・波形・シーンサムネ・自動配置、
rev3)→ UI 初期状態(空状態文言・Claude待機中・16:9追従・実サムネカード)→
detect 21件 → 会話バブル+提案カード(日本語ラベル・内訳・−33.6秒)→
個別「カットする」(rev4、尺·ピル·バブル即同期)→ 一括「無音候補20件を
カット」(rev5、1:21.1→0:48.3、confirm 通過、待機中へ復帰)。

| # | ステップ | 事象 | 重大度 | 根拠 | 修正先 |
|---|---|---|---|---|---|
| F-s1-1 | 個別カット承認 | 候補がクリップ冒頭0.1sから始まる場合、先頭に0.1秒の断片クリップが残る(素材1 · 0:00.1)。以後の一括でも 0.1〜0.4s 断片が多数発生 | P2 | rev4 後のタイムライン(クリップ一覧に 0:00.1/0:00.3 等) | ops: removeSourceRange で minClip 閾値未満の断片を隣接カットに吸収 |
| F-s1-2 | 文字起こしタブ | per-source 転写ボタンは存在するが行テキストにしか見えない(「未実行」の行=ボタンだと気づけない。説明文は「上のボタンで」と言う) | P2 | スクショ(文字起こしタブ) | web: 行内に明示的なボタン様式(枠+ラベル「文字起こしする」) |
| F-s1-3 | detect の適用判断 | 発話のない街歩き素材に無音検出を使うと断片化したカット列になる(編集判断としては scenes/culling が適切)。システムは何も警告しない | P2(知見) | rev5 のクリップ断片化 | プレイブック強化+将来: 発話密度が低い素材で detect 実行時に「シーン選別の方が向いています」ヒント |
| **F-s1-4** | 最終レンダー | **whisper の幻覚タグ「[MÚSICA DE FUNDO]」がそのまま字幕として焼き込まれた**(非発話音声の転写でブラケット系タグが cue 化)。低信頼フラグは上がっていたが書き出しは素通し | **P1** | verify-s1-final.mp4 の 0:42〜47 フレーム | core: 非発話タグ([…]/(…)/♪ 等の多言語パターン)を cue 生成から除外+除外件数を render warnings に載せて結果カードへ |
| F-s1-5 | 文字起こしタブ | 転写ボタンが初回表示時に見えない(転写完了後の再描画では明示ボタンが出た)。初回描画経路の様式不一致の疑い | P3(要再現) | スクショ2枚の差 | web: 未実行行のボタン様式を常時明示 |

**シナリオ1 判定: 合格(条件付き)** — 取り込み→検出→承認→転写→BGM→QC→
4K書き出し→結果カードまで会話+UIで完走、行き止まりゼロ、P0ゼロ。
P1×1(幻覚字幕)は修正後に同シナリオの字幕部分のみ再走で確認する。
